# Titanic Data Prediction

Simple Streamlit app to explore the Titanic dataset, clean data, visualize, train models, and make predictions.

Run locally:

```
pip install -r requirements.txt
streamlit run streamlit_app.py
```

Place `titanic.csv` in the same folder before running.
